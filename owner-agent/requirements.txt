# Pinned to exact versions (2026-08-19) — see backend/requirements.txt's
# comment for why (was previously `>=`-only, no upper bound).
fastapi==0.141.1
uvicorn[standard]==0.52.1
PyJWT==2.13.0
pydantic==2.13.4
httpx==0.28.1
json-repair==0.62.0
